{
  "name": "agent-ready",
  "private": true,
  "packageManager": "pnpm@10.18.1",
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
